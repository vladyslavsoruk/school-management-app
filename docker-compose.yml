version: "3.8"

services:
  postgres:
    image: postgres:15
    container_name: school_management_system
    environment:
      POSTGRES_USER: vladsoruk
      POSTGRES_PASSWORD: vladsoruk12345
      POSTGRES_DB: school
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  web:
    build:
      context: .
      args:
        DATABASE_URL: postgresql://vladsoruk:vladsoruk12345@localhost:5432/school
    container_name: nextjs_app
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL: postgresql://vladsoruk:vladsoruk12345@localhost:5432/school
    depends_on:
      - postgres
